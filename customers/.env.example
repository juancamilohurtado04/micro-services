PORT=8001
DB_URL=mongodb://localhost:27017/customers

# Secreto con el que se FIRMAN los JWT. customers es el unico servicio que
# firma; shopping usa este mismo valor solo para verificar. No lo escribas
# aqui: copia este archivo a .env (ignorado por git) y ponlo alli.
APP_SECRET=
